# 智能面试辅导系统 Q&A

## 当前版本更新说明

下面这部分是基于项目当前代码实现补充的“最新版口径”。  
原文中如果有和下面冲突的旧说法，优先以这里为准。

### 1. 这个项目现在的用户体系是什么

当前已经不是“只输入用户名”的轻量模式了，而是正式登录体系。

可以直接记这 5 点：

1. 支持邮箱 + 密码注册登录  
2. 用户表和会话表保存在 SQLite  
3. 后端登录成功后创建 session  
4. 前端通过 Cookie 维持登录态  
5. 不同用户的问答、面试、报告和历史记录是隔离的

标准回答：

当前项目已经升级成正式登录体系。用户会先通过邮箱和密码注册或登录，后端把用户信息和登录会话保存到本地 SQLite 里。登录成功后，后端通过 Cookie 维持当前会话，后续所有问答、模拟面试、简历上传、历史记录和报告下载，都会绑定到当前登录用户，所以现在已经具备比较标准的用户隔离能力。

### 2. 用户信息、历史消息和报告现在分别存在哪里

可以直接记这 4 点：

1. 用户和历史记录主数据在 SQLite  
2. 当前工作态快照在本地 JSON  
3. 报告文本会单独导出成 Markdown 文件  
4. 三者一起配合，兼顾恢复能力和正式产品数据沉淀

标准回答：

当前项目里，用户表、登录会话表和历史面试记录表保存在 SQLite；而当前用户正在进行中的问答历史、面试状态、简历文本和当前报告这些“工作态快照”，仍然保存在 `data/user_histories` 目录下的 JSON 文件里。除此之外，面试报告生成后还会单独导出成 Markdown 文件，保存在 `data/interview_reports` 目录下。也就是说，现在是 SQLite + JSON + 报告文件三层落地。

### 3. 前端和后端现在是怎么鉴权的

可以直接记这 4 点：

1. 前端登录时调用注册 / 登录接口  
2. 后端校验成功后写入 Cookie  
3. 后续请求自动带上 Cookie  
4. 后端根据 Cookie 找到当前用户

标准回答：

现在这个项目没有把 token 存在 localStorage 里，而是采用 Cookie 会话方式。前端登录后，后端会创建 session，并把会话标识写到 Cookie 中；之后浏览器会自动带着这个 Cookie 去访问后端接口，后端再根据 Cookie 找到当前登录用户。这种方式比较适合同域部署的中小型 Web 应用。

### 4. 这个项目现在的流式输出是伪流式还是真流式

现在已经不再是伪流式，而是真流式。

可以直接记这 5 点：

1. 后端返回的是 HTTP 流式响应  
2. 数据按 `status / chunk / done / error` 事件分段返回  
3. 前端通过 `fetch + response.body.getReader()` 持续读取  
4. 每拿到一段 chunk 就更新最后一条 assistant 消息  
5. 所以不是前端定时器假装输出，也不是 WebSocket

标准回答：

当前项目的聊天和模拟面试回复，已经升级成基于 HTTP 的真实流式响应。后端不是一次性把完整答案返回给前端，而是按行持续返回 `status`、`chunk`、`done` 这类事件；前端通过 `fetch + ReadableStream + getReader()` 去持续读取数据流，每拿到一段文本就立即更新页面，所以现在是真流式，不是之前那种前端定时器模拟出来的伪流式效果。

### 5. 普通请求和流式请求分别怎么走

可以直接记这 4 点：

1. 普通接口走 `axios`  
2. 文件上传走 `axios + FormData`  
3. 流式回复走 `fetch`  
4. 因为 `fetch` 更方便直接读浏览器流数据

标准回答：

这个项目里，普通的业务请求，比如登录、生成报告、获取历史记录、上传知识库和简历，主要是通过 axios 封装统一处理的；文件上传场景走的是 `FormData`。而真正的流式问答和流式面试回复，保留的是原生 fetch，因为前端需要直接读取 `response.body` 的流数据，这部分 fetch 更适合配合 `getReader()` 使用。

### 6. 现在新增了哪些更像正式产品的功能

可以直接记这 6 点：

1. 正式注册登录  
2. SQLite 用户与历史记录  
3. 历史面试记录列表  
4. 报告文件导出与下载  
5. 继续生成 / 重试本轮回答  
6. 前端体验优化：长列表、自动滚动、骨架屏、懒加载、移动端适配

标准回答：

这版项目相较于早期 Demo，最大的升级是把它往正式产品方向推进了。除了原本的问答、模拟面试、简历定制化提问和报告生成，现在还补上了正式注册登录、SQLite 用户和历史记录管理、报告文件导出、历史面试记录列表、继续生成 / 重试本轮回答，以及前端长列表优化、自动滚动、骨架屏、懒加载和移动端适配这些工程化能力。

axios 封装一般怎么做
通常分 4 步：
1. 创建 axios 实例
统一配置：
baseURL
timeout
默认请求头
例如：
const service = axios.create({
  baseURL: "/api",
  timeout: 10000,
});
2. 请求拦截器
在每次请求发出去前统一处理，比如自动带 token：
service.interceptors.request.use((config) => {
  const token = localStorage.getItem("token");
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});
3. 响应拦截器
统一处理后端返回格式和错误：
service.interceptors.response.use(
  (response) => response.data,
  (error) => Promise.reject(error)
);
这样页面里调用接口时就不用每次写很多重复代码。
4. 再封装业务接口
比如：
export function login(data) {
  return service.post("/token", data);
}

export function getUserInfo() {
  return service.get("/users/me");
}
然后页面里只调用这些函数，不直接写具体请求。

## 用户点击“开始面试”按钮：
1. 用户点击“开始面试”按钮  
2. Vue 的 @click 触发 startInterview  
3. startInterview 方法里写死调用 /api/interview/start  
4. 同时传 user_id 和 role  
5. 后端返回第一题和最新状态

## 1. 你先整体介绍一下这个项目。

1. 这是一个前后端完整 Web 系统 ，面向技术面试场景的前后端完整 Web 系统 
2. 前端用 Vue 3 + JavaScript + CSS  实现页面和交互
3. 后端用 FastAPI + Python   提供接口和业务逻辑
4. AI 能力基于 LangChain + DeepSeek + Chroma + RAG  
5. 主要场景是技术问答、模拟面试、简历定制化提问、评分和报告生成
6. 使用的时候，看下面从前端到后端的完整链路

## 4. 这个项目从前端到后端的完整链路是什么？

**可以直接记这 6 步**

1. 进入系统时： 用户先输入用户名进入系统  
2. 前端调用加载用户接口，后端存在就恢复，不存在就创建  
3. 前端把返回的数据写入响应式状态，页面完成初始化  
4. 后续交互时： 用户可切换问答模式或模拟面试模式  
5. 发送消息时前端发 HTTP 请求，后端处理后返回结果或流式数据  
6. 前端更新状态并重新渲染页面

## 2. 这个项目实现了哪些核心功能？

**可以直接记这 6 点**

1. 技术问答模式  
2. 模拟面试模式  
3. 简历上传后定制化提问  
4. 知识库导入和 RAG 检索问答  
5. 面试评分和报告生成  
6. LangSmith 调试与历史状态恢复

**标准回答**

这个项目主要有两大核心使用场景。第一个是问答模式，用户可以围绕技术知识点、面试题、岗位准备进行提问，系统会结合知识库和模型能力回答；第二个是模拟面试模式，用户可以选择目标岗位，也可以上传简历，系统会根据岗位要求或者“岗位 + 简历”组合来提问。  
除此之外，项目还支持知识库文档导入、RAG 检索增强、面试过程中的评分、结束后的面试报告生成，以及 LangSmith tracing 调试和本地历史状态恢复。

---

## 5. 这个项目的用户体系是怎么做的？是登录系统吗？

**可以直接记这 4 点**

1. 当前已经是账号密码登录  
2. 支持邮箱 + 密码注册登录  
3. 用户和会话存在 SQLite  
4. 后续请求通过 Cookie 识别当前用户

**标准回答**

当前项目现在已经升级成正式登录体系。用户会通过邮箱和密码注册或登录，后端在 SQLite 里保存用户信息和登录会话。登录成功后，后端通过 Cookie 维持当前会话，后续所有问答、模拟面试、简历上传、历史记录和报告下载，都会绑定到当前登录用户，所以现在已经具备比较标准的用户隔离能力。

---


## 用户信息那些是存在哪里的（历史消息等），并且怎么状态持久化，怎么实现存储的这个项目里
当前项目现在是分层存储。用户表、登录会话表和历史面试记录表保存在 SQLite；而当前用户正在进行中的问答历史、面试状态、简历文本和当前报告这些“工作态快照”，仍然保存在 `data/user_histories` 目录下的 JSON 文件里。除此之外，面试报告生成后还会单独导出成 Markdown 文件，保存在 `data/interview_reports` 目录下。  
“现在是 SQLite + JSON + 报告文件三层落地。SQLite 负责正式业务数据，JSON 负责当前工作态恢复，Markdown 文件负责报告导出和本地留存。”

## 那为什么不实现把token存到localstorage？
“当前项目现在没有把 token 存到 localStorage，主要是因为它走的不是 JWT + localStorage 方案，而是 Cookie 会话方案。用户登录成功后，后端会创建 session，并把会话标识写到 Cookie 里；后续浏览器会自动带着 Cookie 访问接口，后端再根据 Cookie 找到当前登录用户。这种方式在当前这个同域部署的 Web 项目里更简单，也更适合做正式登录态。”

## token 是后端在用户登录成功后生成的一段身份凭证字符串（登录态标识），不是前端自己随便写的。 
用户输入邮箱和密码后，前端把登录信息发给后端；后端先去 SQLite 查这个用户，再校验密码。如果校验成功，后端就会创建一个 session，并把会话标识写进 Cookie。以后前端再请求“获取当前用户”“获取历史记录”“上传简历”这些需要登录态的接口时，浏览器会自动带上 Cookie，后端再根据 Cookie 去识别当前用户。

---

## 补充基础题速记

## 1. Vue2 和 Vue3 有什么区别？

可以直接记这 5 点：

1. Vue3 性能更好  
2. Vue3 更强调 Composition API  
3. Vue3 对 TypeScript 支持更好  
4. Vue3 响应式底层从 `Object.defineProperty` 变成了 `Proxy`  
5. Vue3 更适合中大型工程和逻辑复用

标准回答：

Vue2 和 Vue3 的核心思想都是组件化和响应式，但 Vue3 在性能、逻辑复用和工程化支持上更强。比较大的区别是 Vue3 增加了 Composition API，可以把复杂业务逻辑按功能维度抽到 composable 里复用；另外它的响应式底层改成了 Proxy，对新增属性、删除属性和对象监听支持更自然。对我这个项目来说，Vue3 比较适合承接登录态、问答、模拟面试、历史记录这些复杂状态和交互逻辑。

## 2. ES6+ 新特性常见有哪些？

可以直接记这 8 个：

1. `let` / `const`  
2. 箭头函数  
3. 模板字符串  
4. 解构赋值  
5. 展开运算符 `...`  
6. Promise / async / await  
7. 模块化 `import / export`  
8. 类、默认参数、可选链这些增强语法

标准回答：

ES6+ 常见新特性主要包括 let 和 const、箭头函数、模板字符串、解构赋值、展开运算符、Promise 和 async/await，还有 import/export 模块化。实际开发里最常用的是这几类，因为它们直接影响组件开发、接口请求、状态更新和代码组织方式。

## 3. const 和 let 有什么区别？

可以直接记这 4 点：

1. 都有块级作用域  
2. `let` 可以重新赋值  
3. `const` 不能重新指向新值  
4. `const` 如果是对象，里面的属性仍然可以改

标准回答：

`let` 和 `const` 都是 ES6 引入的块级作用域变量声明方式。区别在于 `let` 声明的变量后续可以重新赋值，而 `const` 声明后不能再重新赋值。不过如果 `const` 保存的是对象或数组，它里面的属性和元素还是可以修改的，只是这个变量本身不能再指向新的对象。

## 4. Git 基本怎么用？

可以直接记这 7 个命令：

1. `git clone`
2. `git pull`
3. `git status`
4. `git add`
5. `git commit -m "xxx"`
6. `git push`
7. `git branch`

标准回答：

Git 是最常用的版本管理工具。日常开发里，我最常用的是先 `git clone` 拉代码，开发前 `git pull` 更新最新版本，改完之后通过 `git status` 看变更，再 `git add`、`git commit`、`git push` 提交。如果需要多人协作或者功能隔离，还会用 `git branch` 创建分支。把代码上传到 GitHub，本质上也是通过 Git 把本地仓库推送到远端仓库。

## 5. 网络通信常见有哪些？

可以直接记这 5 类：

1. HTTP / HTTPS  
2. AJAX / axios / fetch  
3. SSE  
4. WebSocket  
5. Cookie / Session / Token 这种认证相关通信机制

标准回答：

前端常见的网络通信方式主要还是基于 HTTP，比如用浏览器 fetch 或 axios 请求后端接口；如果是需要服务端持续推送数据，常见会有 SSE 或 WebSocket。像我这个项目，普通接口主要走 HTTP + axios，流式问答和流式面试回复走的是基于 HTTP 的流式响应，前端再通过 fetch 持续读取流数据，不是 WebSocket。

## 6. 如果给你一个任务，你怎么去完成？

可以直接记这 6 步：

1. 先明确需求和边界  
2. 拆页面、状态、接口和交互  
3. 先确认需要改前端、后端还是联调  
4. 借助 AI 工具生成初版实现  
5. 自己跑通、验证、debug  
6. 最后整理代码和结果

标准回答：

如果给我一个任务，我一般会先确认需求边界，比如这个功能给谁用、输入输出是什么、核心链路是什么。然后我会把它拆成页面结构、状态管理、接口调用和异常处理几个部分，判断需要改前端、后端还是前后端联调。实现时我会合理借助 AI Coding 工具提高效率，比如帮助我生成初版代码、补测试思路或排查报错，但不会直接照搬，最后一定会自己本地跑通功能，看接口、看状态更新、看边界情况，把结果验证清楚。

## 7. 如果要用 AI 工具输入指令来完成任务，你怎么做？

可以直接记这 4 点：

1. 先把需求描述清楚  
2. 明确技术栈、文件位置、输入输出  
3. 让 AI 先给方案或局部实现  
4. 自己再验证结果和修正

标准回答：

如果我借助 AI 工具完成开发任务，我不会只给一句很模糊的话，而是会把需求、技术栈、当前文件结构、希望改动的范围、输入输出格式都描述清楚。比如我会告诉它这是 Vue 3 还是 FastAPI，要改哪个组件或哪个接口，要保持哪些已有逻辑不变。这样 AI 生成的结果会更可控。生成之后，我会自己检查代码逻辑、实际运行效果、接口返回和异常场景，确保结果真的满足需求。

## 8. 弹性布局有哪些？

可以直接记这 4 类：

1. Flex 布局  
2. Grid 布局  
3. 百分比 / vw / vh / rem 这些相对单位  
4. 媒体查询做响应式布局

标准回答：

前端里常见的弹性布局方式，最常用的是 Flex 和 Grid。Flex 更适合一维布局，比如一行按钮、一列卡片、左右结构；Grid 更适合二维布局，比如复杂面板和卡片矩阵。另外，为了适配不同屏幕，还会结合百分比、rem、vw、vh 这些相对单位，以及媒体查询去做响应式布局。像我这个项目里，很多区域是用 Flex 和 Grid 组合实现的，同时在样式里补了移动端媒体查询。

## 9. Flex 常见属性你要会哪些？

可以直接记：

- 容器上：`display: flex`
- 主轴方向：`flex-direction`
- 主轴对齐：`justify-content`
- 交叉轴对齐：`align-items`
- 是否换行：`flex-wrap`
- 子项伸缩：`flex`

标准回答：

Flex 最核心的是先把容器设成 `display: flex`，然后通过 `flex-direction` 决定横向还是纵向排布，通过 `justify-content` 控制主轴对齐，通过 `align-items` 控制交叉轴对齐，通过 `flex-wrap` 决定是否换行。子元素再通过 `flex` 控制占比和伸缩能力。

## 10. Grid 常见属性你要会哪些？

可以直接记：

- `display: grid`
- `grid-template-columns`
- `grid-template-rows`
- `gap`
- `grid-column`
- `grid-row`

标准回答：

Grid 更适合做二维布局。最常用的是 `grid-template-columns` 和 `grid-template-rows` 来定义行列结构，再配合 `gap` 控制间距。如果某个元素需要跨列或跨行，可以用 `grid-column` 和 `grid-row`。像历史记录页这种左右两栏结构，其实就很适合用 Grid。

## 6. 用户历史为什么关闭页面后还不会丢？

**可以直接记这 4 点**

1. 前端状态不是唯一来源  
2. 后端把用户状态持久化到本地 JSON  
3. 同一用户名会重新读回这个文件  
4. 所以刷新页面或重启服务后还能恢复

**标准回答**

因为这个项目不是只把状态放在前端内存里，而是把用户状态持久化到了后端本地 JSON 文件。每个用户名对应一个独立的状态文件，里面会保存问答历史、面试历史、简历文本、面试分数、报告和面试状态。  
所以页面关闭、浏览器刷新，甚至后端服务重启后，只要这个用户名对应的 JSON 文件还在，下次输入同一个用户名就能把之前的数据恢复出来。

---

## 7. 前端为什么用 Vue 3？你前端做了什么？

**可以直接记这 5 点**

1. 用 Vue 3 做组件化  
2. 把不同模块拆成独立组件  
3. 用 composable 统一管理状态  
4. 用 JavaScript 处理交互逻辑  
5. 完成前后端联调

**标准回答**

前端我是用 `Vue 3` 来实现的，整体采用组件化开发方式，把侧边栏、顶部模式栏、问答面板、模拟面试面板、聊天历史区这些功能拆成独立组件，最后在 `App.vue` 里统一组合。  
业务逻辑上，我把核心状态和方法集中放在 `useInterviewApp.js` 这个 composable 里处理，比如用户名加载、模式切换、文件上传、消息发送、流式响应读取、手动停止生成这些。这样页面组件更偏展示层，业务状态和接口交互更集中，结构会更清晰。

**关键词提示**  
`组件化 / 业务状态 / 接口交互逻辑 / 联调`

---

## 8. 模式切换是怎么实现的？

**可以直接记这 4 步**

1. 前端维护一个 `mode` 状态变量  
2. 点击侧边栏按钮时修改 `mode`  
3. 页面根据 `mode` 做条件渲染  
4. 自动显示问答面板或面试面板

**标准回答**

模式切换本质上是一个前端状态切换。项目里维护了一个 `mode` 响应式状态，用来标记当前是问答模式还是模拟面试模式。用户点击侧边栏里的模式按钮时，会修改这个 `mode`。  
页面层面则根据 `mode` 做条件渲染，比如 `mode === QA_MODE` 时显示问答面板，否则显示模拟面试面板。所以切换模式时不需要刷新页面，只需要改一个状态，Vue 就会自动把对应面板切换出来。

---

## 9. 前端怎么获取后端数据？

1. 用户在页面上点击、输入、上传文件，触发前端事件函数
2. 事件函数去调用封装好的接口方法，请求后端接口
3. 普通接口请求体一般走 JSON提交，文件上传接口走 FormData 
4. 后端处理后，普通接口返回 JSON，聊天接口返回流式响应 
5. 前端拿到结果后，会先解析响应，把数据写回响应式状态，再由 Vue 自动更新页面

（我这个项目里，大部分接口都是普通 JSON 交互，比如登录、获取当前用户、获取会话列表、获取历史消息。文件上传是特殊情况，因为要传文件，所以用 FormData。流式聊天又是另一种特殊情况，它发送给后端时请求体仍然是 JSON，但后端返回的不是一次性 JSON，而是持续返回的数据流，前端再边读边更新页面。）

**相关代码**

- 前端请求封装：[frontend/src/api/client.js](D:/GZHU/基于%20RAG%20与%20Agent%20的智能面试辅导系统/frontend/src/api/client.js)
- 前端核心状态逻辑：[frontend/src/composables/useInterviewApp.js](D:/GZHU/基于%20RAG%20与%20Agent%20的智能面试辅导系统/frontend/src/composables/useInterviewApp.js)

**关键词提示**  
`事件函数 -> axios / fetch -> JSON/FormData -> 更新 state`

---

## 10. 前端如何感知状态变化？

**可以直接记这 5 步**
（ref 放“原始数据”，computed 放“根据原始数据算出来的结果”。）
ref 更适合放那些会被直接修改的原始状态，比如当前用户、当前模式、输入框内容这些；computed 更适合放根据这些原始状态推导出来的结果，比如当前应该展示哪段历史、是否显示欢迎区。这样状态层次会更清晰，也能避免重复存储和数据不一致

1. 核心状态放在 Vue 响应式对象里，核心状态通过 ref 定义，派生状态通过 computed 管理  
2. 用户操作后触发前端事件和请求    
3. 后端返回新数据或流式数据  
4. 前端修改响应式状态  
5. Vue 自动通知组件重新渲染（Vue 自动追踪依赖，让用到这些状态的组件重新渲染）

**标准回答**

前端感知状态变化，本质上依赖的是 Vue 的响应式系统。在这个项目里，像 candidate、mode、qaInput、interviewInput、typingState 这些核心状态，主要是通过 ref 管理的；而像当前显示的历史记录、欢迎区是否展示这类派生结果，则是通过 computed 管理的。
用户操作后，前端会发起请求，后端返回新数据或流式数据后，前端再把结果更新到这些响应式状态。状态一旦变化，Vue 就会自动追踪依赖关系，让对应组件重新渲染，所以页面内容会自动更新，不需要手动操作 DOM。

**关键词提示**  
`ref / computed / 请求返回后改 state / 自动重新渲染`

---

## 11. 问答模式下，前端怎么拿到后端结果？流式响应、读取器

**可以直接记这 6 步**

1. 用户输入问题  
2. 前端先显示用户消息  
3. 前端插入一条 assistant 占位消息  
4. 前端调用流式问答接口 `/api/qa/chat/stream`  
5. 前端拿到响应后，会通过 `response.body.getReader()` 拿到读取器，然后调用read（）方法去持续读取 chunk
6. 用 `TextDecoder` 解码后再逐条解析事件
7. 每读取到一段内容，就把它追加到最后那条 assistant 消息里，所以一边更新消息内容，一边展示到页面上

不是轮询或者多久刷新一次

**关键词提示**  
`先占位，再请求，再流式读取，再更新消息`


## 13. 后端如何通知前端“我的数据更新了”？

**可以直接记这 4 点**

1. 当前不是 WebSocket 主动推送  
2. 普通接口靠 HTTP 请求-响应  
3. 流式接口靠 HTTP 流持续返回 chunk  
4. 前端读到数据后自己更新状态

**标准回答**

当前项目里，后端不是通过 WebSocket 主动通知前端，而是基于标准的 HTTP 通信模式。普通接口下，前端先发请求，后端处理完成后通过 JSON 响应把最新状态返回给前端；流式接口下，后端则通过 `StreamingResponse` 持续把新的文本块推到响应体里。  
前端一旦收到这些响应，不管是普通 JSON 还是流式 chunk，都会把结果写回自己的响应式状态，所以页面就能同步更新。

**关键词提示**  
`普通接口看 JSON，流式接口看 chunk`

---

## 14. 文件上传交互是怎么实现的？

**可以直接记这 5 步**

1. 用户选择文件  
2. 前端先保存文件对象  
3. 用 `FormData` 组装请求  
4. 调用上传接口  
5. 后端返回新状态，前端刷新页面

**标准回答**

文件上传分成简历上传和知识库导入两类，但实现思路是一致的。用户选择文件以后，前端先把文件对象保存在状态里，然后通过 `FormData` 组装请求，把文本字段和文件对象一起 append 到 FormData 里，然后通过 axios 发给后端，再调用对应的后端接口。  
比如简历上传走 `/api/resume/upload`，知识库导入走 `/api/knowledge/import`。后端处理完以后，会返回新的用户状态或者导入结果，前端再把状态写回去，页面就能立即展示新的简历状态或导入反馈。


**关键词提示**  
`选文件 -> FormData -> 上传接口 -> 页面刷新`

---
FormData 是浏览器原生提供的一个官方 Web API，主要用来组装表单数据，特别适合文件上传场景。前端可以把文本字段和文件对象一起 append 到 FormData 里，然后通过 axios 发给后端

## formdata代码
const formData = new FormData();
formData.append("user_id", userId);
formData.append("file", file);
然后再发请求：
axios.post("/api/upload", formData);



## 实现 axios 普通请求代码
这个项目里封装普通 POST 请求，大致就是这种思路：
## 封装

把 POST 请求统一封装成一个方法，里面负责通过 axios 发请求、设置传送的数据格式是 json：`Content-Type`、把对象交给 axios 转成 JSON，再解析后端响应。这样页面层只管传路径和参数，不用重复写底层请求逻辑。

export async function apiPost(path, body) {
   //  body是请求体
  const response = await request.post(path, body, {
    headers: {
      "Content-Type": "application/json",
    },
  });
  return response.data;
}

然后页面里真正发送问答消息时，就会调用类似：
apiPost("/api/interview/start", {
  user_id: candidate.value.user_id,
  role: selectedRole.value,
});

“这里 axios 普通请求封装主要做了几件事：
第一，指定请求地址；
第二，指定请求方法是 POST；
第三，设置请求头告诉后端这次传的是 JSON；
第四，把前端的数据通过 JSON.stringify 转成字符串放进请求体；
第五，等待后端返回响应，再把响应解析成 JSON 给前端使用。

“比如用户点击开始面试时，前端会把当前用户名和目标岗位通过 axios 普通请求发给后端的 `/api/interview/start` 接口；后端返回第一道题和最新状态后，前端再把这些数据写入响应式状态，页面就会自动更新。”


## 15. 后端整体是怎么设计的？

**可以直接记这 5 点**

1. FastAPI 做接口层  
2. Service 层做业务逻辑  
3. LangChain 组织模型能力  
4. Chroma 做向量检索  
5. JSON 做用户状态持久化

**标准回答**

后端整体是一个比较典型的分层设计。FastAPI 负责接口层，接收前端请求并返回 JSON 或流式响应；核心业务逻辑集中在 `InterviewAssistantService` 里，负责问答、模拟面试、评分和报告生成。  
知识库部分通过 Chroma 做向量检索；用户的问答历史、面试历史、面试状态、分数和报告，则持久化到本地 JSON 文件里。这样接口层比较轻，业务逻辑更集中，后续扩展也更方便。

**关键词提示**  
`接口层 / 业务层 / RAG / 持久化`

---

## 16. 问答接口的完整链路是什么？#问答

**可以直接记这 6 步**

1. 前端调用流式聊天的接口， `/api/qa/chat/stream`  
2. 后端读取当前用户状态  
3. 把用户消息追加到 `qa_history`  
4. 调用问答服务生成回复  
5. 通过流式接口持续返回文本块  
6. 完成后把最终回答写回历史并持久化

**标准回答**

问答接口的链路比较清晰。前端调用 `/api/qa/chat/stream` 之后，后端先读取当前用户名对应的用户状态，然后把用户这轮消息写入 `qa_history`。接着后端调用 `InterviewAssistantService` 里的问答逻辑去生成回答。  
回答生成后，后端不是一次性把完整内容返回给前端，而是通过流式响应的方式把文本块持续返回出去。等整轮结束后，后端再把最终 assistant 回复写入 `qa_history`，并把整个用户状态持久化到本地文件里。

**关键词提示**  
` 发送调用聊天接口 -》读状态 -> 写用户消息 -> 问答服务，生成回复 -> 流式返回 -> 持久化`

---

## 17. 模拟面试接口的完整链路是什么？

**可以直接记这 6 步**

1. 前端先调用开始面试接口  
2. 后端初始化面试状态机  
3. 生成第一题并返回  
4. 用户回答后调用 `/api/interview/message/stream`  
5. 后端结合评分和状态机决定下一步  
6. 返回追问、提示、下一题或结束

**标准回答**

模拟面试分开始和过程两部分。开始面试时，前端调用 `/api/interview/start`，后端根据当前岗位和简历情况初始化面试状态机，然后生成开场白和第一道题返回给前端。  
后续每一轮用户回答时，前端调用 `/api/interview/message/stream`。后端会先把这轮回答写入面试历史，再结合用户当前回答、面试状态、评分结果和状态机规则，决定下一步是继续追问、给提示、切到下一题还是结束面试，最后把结果流式返回给前端。

**关键词提示**  
`先 start，再 message，再状态机决策`

---

## 18. 状态机在项目里起什么作用？

**可以直接记这 4 点**

1. 限制追问次数  
2. 限制总题数  
3. 决定追问、提示还是切题  
4. 决定什么时候结束面试

**标准回答**

状态机在这个项目里，本质上是模拟面试的流程控制器。因为如果完全让模型自由发挥，它很容易一直追着一个问题问，或者提问节奏失控。  
加了状态机以后，就可以明确控制每道题最多追问几次、整场面试总共问多少题、当用户回答不够好时是给提示还是直接切题，以及什么时候结束面试。这样面试过程就更像真实面试官，而不是普通聊天机器人。

**关键词提示**  
`流程控制，不让模型自由失控`

---

## 19. 前端能感知面试状态机的状态吗？

**可以直接记这 3 点**

1. 决策在后端  
2. 前端不决定追问还是切题  
3. 前端通过后端返回的 `candidate.interview_state` 和 `interview_finished` 感知状态

**标准回答**

前端本身不做状态机决策，真正决定“继续追问、切题还是结束”的是后端。前端主要是感知和展示这些结果，也就是通过后端返回的 `candidate.interview_state`、`interview_finished`、`interview_score` 这些字段，知道当前面试推进到了什么阶段。  
所以你可以理解成：状态机控制权在后端，前端负责把后端已经计算好的状态呈现出来。

---

## 20. RAG 在这个项目里怎么实现？

**可以直接记这 6 步**

1. 用户上传知识库文档  
2. 后端保存文件并解析文本  
3. 对文本做切分  
4. 调用 embedding 模型向量化  
5. 写入 Chroma  
6. 问答时先检索，再生成答案

**标准回答**

RAG 的链路是这样的：用户上传知识库文档后，后端先把文件保存下来，再解析出文本内容；然后把文本按块切分，调用 embedding 模型生成向量，并写入 Chroma 向量库。  
后续当用户提问时，系统会先在 Chroma 里检索出最相关的文档片段，再把这些片段拼进 Prompt，让模型基于检索结果生成最终回答。

**关键词提示**  
`上传 -> 解析 -> 切分 -> 向量化 -> 检索 -> 生成`

---

## 21. 无简历时，系统根据什么来提问？

**可以直接记这 4 点**

1. 主要根据目标岗位  
2. 结合知识库里的技术题和知识点  
3. 再参考当前面试进度  
4. 尽量避免连续围绕同一知识点

**标准回答**

如果用户没有上传简历，系统主要根据目标岗位来组织提问，比如前端开发、后端开发、Agent 开发这些岗位。提问内容会结合知识库里的技术题、知识点和岗位维度来生成，同时还会参考当前面试进度和最近问过的问题。  
所以它不是随机出题，而是“岗位要求 + 知识库 + 当前面试状态”一起决定下一题。

---

## 22. Tool Calling 在这个系统里是怎么用的？

**可以直接记这 4 步**

1. 用户提问  
2. 模型先判断需不需要调用工具  
3. 需要的话就调用天气、知识库等工具  
4. 再基于工具结果生成最终回答

**标准回答**

这个系统里的 Tool Calling 主要用在问答模式下。它的作用不是让模型直接凭记忆硬答，而是先判断当前问题是否需要借助外部工具，比如查天气、查知识库或者取其他外部信息。  
如果模型判断需要工具，就先调用对应工具拿到结果，再把工具结果和用户问题一起交给模型生成最终回答。这样系统就不只是一个纯聊天模型，而是具备“按需调用外部能力”的能力。

**关键词提示**  
`先判断要不要调工具，再回答`

---

## 23. Tool Calling 和状态机是什么关系？

**可以直接记这 2 句**

1. Tool Calling 决定“用什么能力”  
2. 状态机决定“下一步做什么动作”

**标准回答**

Tool Calling 和状态机不是一回事。Tool Calling 解决的是：模型回答问题时，需不需要借助天气、知识库之类的外部工具；状态机解决的是：这场模拟面试下一步应该继续追问、给提示、切题还是结束。  
一个偏“能力调用”，一个偏“流程控制”。

**关键词提示**  
`一个管能力，一个管流程`

---

## 24. LangSmith 在项目里起什么作用？

**可以直接记这 4 点**

1. 看 Prompt  
2. 看模型输入输出  
3. 看工具调用过程  
4. 看链路耗时和定位问题

**标准回答**

LangSmith 在这个项目里主要用于可观测性和调试。通过 tracing，我可以看到问答、模拟面试、知识库检索和报告生成这些链路里的 Prompt、模型输入输出、工具调用过程以及整体耗时。  
这样如果某次回答效果不好，或者工具调用不符合预期，我就更容易定位问题到底出在 Prompt、检索、工具调用还是模型输出这一层。

**关键词提示**  
`看链路、排问题、做调试`

---

## 25. Prompt Engineering 在这个项目里做了什么？

**可以直接记这 4 点**

1. 问答系统提示词  
2. Tool Calling 规则提示词  
3. 面试提问和追问提示词  
4. 评分和报告生成提示词

**标准回答**

Prompt Engineering 在这个项目里不是只做聊天提示，而是贯穿了多个环节。比如问答模式的系统提示词、Tool Calling 的规则提示、模拟面试的提问和追问提示，以及面试评分和报告生成提示词。  
也就是说，Prompt 在这个项目里不仅控制回答风格，还承担了任务编排、流程约束、评分和总结的作用。

**关键词提示**  
`不只聊天，还做决策、评分、总结`

---

## 26. 如果用户手动停止、断网、刷新页面，系统怎么处理？

**可以直接记这 4 点**

1. 前端可以手动中断流式请求  
2. 未完成消息会标记为 `interrupted`  
3. 后端加载历史时会修正“生成中”状态  
4. 用户之后可以继续追问或重答

**标准回答**

前端在流式请求时会创建中断控制器 `AbortController`，用户点击停止按钮时会中断这次流式请求，同时把当前这条消息标记成 `interrupted`。  
如果用户是断网、刷新页面或者服务异常中断，后端在下次加载用户状态时，也会扫描历史里的“生成中”消息，并把它们修正成中断状态，避免页面里永远挂着一条未完成消息。这样用户后面还能继续追问或者重新发起回答。

## 系统如果出现了中断或者断网或者后端大模型连接失败的情况，要怎么解决
当前项目已经做了基础兜底：请求中断或失败时，把未完成消息标记成 interrupted，避免页面卡死；后端下次加载历史时也会修正异常状态。更完整的生产级方案，还可以继续补超时控制、自动重试、恢复机制、日志监控和降级策略

第一层是当前项目已经做了什么兜底。
第二层是如果做成更完整的生产级系统，还可以怎么增强。
你可以直接这样答：
“如果系统出现中断、断网或者后端模型连接失败，我会分前端和后端两层处理。前端这边，当前项目已经做了基础兜底：如果流式请求被手动停止、网络中断或者页面刷新，当前未完成消息会被标记成 interrupted，避免界面一直停留在‘生成中’。下次重新进入时，后端也会把历史里处于 generating 状态的消息修正成中断状态，这样用户至少不会丢失上下文，也可以继续追问或者重新发起请求。”
“如果是后端模型连接失败，比如调用大模型接口超时、网络异常、API 报错，后端会捕获异常并返回错误信息，前端再把这条消息显示成失败或中断状态，而不是让页面卡死。这样做的目标是让系统‘优雅失败’，也就是即使这轮回答没成功，页面状态仍然是可控的。”

## 增强
“如果做成更完整的生产级方案，我会加几类机制。第一是超时控制，比如模型长时间没返回就主动结束，并提示用户稍后重试；第二是有限次数重试，比如网络抖动时自动重试 1 到 2 次；第三是恢复机制，把未完成消息和当前会话状态保存下来，用户刷新或重连后能继续恢复；第四是日志和监控，比如记录接口耗时、错误率、模型失败原因，方便排查；第五是降级策略，比如模型服务暂时不可用时，可以提示切换到普通问答、稍后再试，或者返回一个兜底说明，而不是整个系统不可用。”
如果面试官问“
---

## 27. 当前项目里的“评分”是怎么做的？

**可以直接记这 4 点**

1. 每轮面试都会更新状态  
2. 后端结合回答表现和状态机结果计算分数  
3. 分数保存在用户状态里  
4. 结束后再结合整场表现生成报告

**标准回答**

当前项目里的评分是在后端完成的。每一轮面试回答后，后端会结合当前回答表现、面试状态和面试历史去计算当前分数，并把结果写到用户状态里。  
也就是说分数不是最后一次性拍脑袋给出来的，而是在整个面试过程中持续更新。等用户结束面试后，再结合整场对话历史去生成总结性报告。

---

## 28. 如果面试官问：这个项目更像 Agent 项目吗？

**可以直接记这 3 点**

1. 算偏 Agent 应用  
2. 问答模式有 Tool Calling  
3. 面试模式更像状态机 + Prompt 编排

**标准回答**

这个项目可以算偏 Agent 应用，但更准确地说，它是一个带 Tool Calling 和流程控制的 LLM 应用。问答模式下，模型会根据问题判断是否调用工具；模拟面试模式下，则更多依赖状态机和 Prompt 编排来控制流程。  
所以它不是典型的多智能体系统，但确实具备比较强的 Agent 应用特征。

---

## 29. 如果问你这个项目最有含金量的前端点，你重点讲什么？

**可以直接记这 3 点**

1. 流式响应渲染  
2. 状态集中管理  
3. 问答和模拟面试双模式交互

**标准回答**

我觉得这个项目里最值得讲的前端点有三个。第一是流式响应渲染，因为它不是普通表单页面，而是要处理持续返回的数据流；第二是状态集中管理，因为问答、面试、文件上传、评分、历史恢复这些状态都比较多，需要通过 composable 做统一管理；第三是双模式交互，因为问答模式和模拟面试模式虽然共用一套系统，但页面逻辑、用户流程和后端接口都不完全一样。  
这三个点放在一起，比较能体现这个项目不是简单静态页面，而是一个完整的交互式 Web 系统。

---



## 新增产品化前端功能怎么实现？

这部分主要用来回答“你后面是怎么把它做得更像正式产品”的问题。

### 首页落地页和路由跳转怎么实现？

**可以直接记这 5 步**

1. 登录成功后进入首页 `/`
2. 首页展示“技术问答 / 模拟面试 / 历史复盘”三个入口
3. 用户点击入口后调用 `router.push()`
4. 前端跳转到 `/workspace/qa`、`/workspace/interview` 或 `/workspace/history`
5. `App.vue` 根据当前路由和模式渲染对应页面

**标准回答**

我把原来直接进入工作台的方式改成了首页落地页，并引入 `Vue Router` 做页面跳转。登录成功后先进入首页，用户可以选择技术问答、模拟面试或历史复盘。点击某个入口后，前端通过 `router.push()` 修改 URL，跳转到对应的工作台页面。这样做的好处是页面结构更清晰，也更接近真实 Web 产品，而不是所有功能都堆在一个页面里。

### 首次使用引导怎么实现？

**可以直接记这 4 步**

1. 前端初始化时请求 `/api/bootstrap`
2. 后端返回 `workspace.onboarding_completed`
3. 如果没完成，引导弹窗显示
4. 用户跳过或完成后调用 `/api/workspace/onboarding` 保存状态

**标准回答**

首次引导不是纯前端临时状态，而是和用户工作区状态绑定。页面初始化时，前端会从 `/api/bootstrap` 拿到当前用户的 `workspace` 信息，如果发现 `onboarding_completed` 是 `false`，就显示引导弹窗。用户完成或跳过后，前端调用 `/api/workspace/onboarding`，后端把状态保存起来。这样同一个用户下次登录时就不会重复弹出。

### 项目文件夹和会话管理怎么实现？

**可以直接记这 6 步**

1. 左侧边栏展示项目和会话树
2. 用户点击新建、重命名、删除、置顶或切换
3. `WorkspaceExplorer.vue` 把事件传给 `useInterviewApp.js`
4. `useInterviewApp.js` 调用 `/api/workspace/...` 接口
5. 后端更新 JSON 里的工作区数据
6. 前端用返回的新 `workspace` 更新 Pinia，页面自动刷新

**标准回答**

项目和会话管理是为了让系统更像真实的工作台。我在前端用 `WorkspaceExplorer.vue` 展示项目文件夹和会话列表，具体的新建、重命名、删除、置顶、切换等动作不会直接在组件里写死，而是通过事件交给 `useInterviewApp.js` 处理。`useInterviewApp.js` 再调用后端的工作区接口，后端更新本地 JSON 状态后返回最新的 `workspace` 和当前会话状态。前端把这些数据写入 Pinia，Vue 响应式系统会自动更新侧边栏和主页面。

### 切换会话后，页面数据怎么变？

**可以直接记这 5 步**

1. 用户点击某个会话
2. 前端调用激活会话接口
3. 后端把该会话设为当前会话
4. 后端返回该会话对应的候选人状态
5. 前端更新 `candidate`，聊天记录和面试状态自动变成该会话的数据

**标准回答**

切换会话时，前端不是只改一个页面高亮，而是会调用后端的激活会话接口。后端会把这个会话设为当前活跃会话，并取出这个会话自己的问答历史、面试历史、简历、报告等状态。接口返回后，前端更新 Pinia 里的 `candidate` 和 `workspace`，所以聊天区、面试区、上传状态都会自动切换到对应会话的数据。

### 主题模式怎么实现？

**可以直接记这 4 步**

1. 侧边栏选择主题
2. 前端更新 Pinia 里的 `themeMode`
3. `App.vue` 给根容器加不同主题 class
4. `styles.css` 根据 class 切换颜色和视觉风格

**标准回答**

主题模式目前主要是前端体验层的设计。我把“严肃面试官、轻量练习、冲刺复习”作为主题枚举存在常量里，用户选择后更新 Pinia 的 `themeMode`。`App.vue` 根据这个状态给页面根容器添加不同 class，CSS 再根据 class 改变颜色、卡片和强调样式。这个设计后续也可以继续扩展，让主题同时影响面试官 Prompt 的语气。

---

## 30. 如果只剩最后 3 分钟，优先背哪几题？

**优先背这 8 个**

1. 先整体介绍一下这个项目  
2. 这个项目实现了哪些功能  
3. 从前端到后端的完整链路是什么  
4. 前端怎么获取后端数据  
5. 前端如何感知状态变化  
6. 真流式回复怎么实现  
7. RAG 怎么实现  
8. Tool Calling 和状态机分别做什么

---

## 31. 最后速记版

- 项目定位：技术问答 + 模拟面试 + 简历定制化 + RAG + 报告生成  
- 前端核心：Vue 3 组件化 + Vue Router 页面跳转 + Pinia 状态管理 + axios 联调 + fetch 真流式渲染  
- 后端核心：FastAPI 接口层 + Service 业务层 + SQLite 用户体系 + JSON 工作区/会话持久化  
- 用户体系：邮箱密码注册登录 + Cookie 会话鉴权 + 不同用户数据隔离  
- 流式核心：后端 `StreamingResponse`，前端 `getReader().read()`  
- RAG 核心：上传文档 -> 解析 -> 切分 -> 向量化 -> Chroma -> 检索生成  
- Tool Calling 核心：模型先判断要不要调工具，再生成答案  
- 状态机核心：控制追问、切题、结束，不让模型一直追着一个点问
