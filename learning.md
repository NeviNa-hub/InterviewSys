# 前端实习一天速补学习清单

这份文档不是为了系统性学完前端，而是为了让你在 **一天内** 补上最容易被面试官问到、也最影响通过率的内容。  
目标不是变成很强的前端，而是做到：

1. 基础概念能讲清楚  
2. 能结合项目说实现  
3. 面试官问到联调、调试、工程化时不慌  
4. 能体现你有学习能力、交付能力和成长潜力

---

# 一、今天最该优先补什么

如果你时间非常紧，优先级建议这样排：

1. 前端基础概念  
2. Vue 常用机制和项目里的对应实现  
3. HTTP、JSON、FormData、文件上传、前后端联调  
4. 工程化命令和调试方法  
5. Git 基本使用  
6. 怎么表达自己会用 AI 工具，但不是完全依赖

你可以把今天的目标理解成一句话：

**先把“页面怎么写、请求怎么发、数据怎么回来、页面怎么更新、报错怎么排查”这条链路讲顺。**

---

# 二、前端基础

## 1. HTML / CSS / JavaScript 分别是干什么的

### 可以直接记这 3 句

1. HTML 负责页面结构  
2. CSS 负责页面样式  
3. JavaScript 负责交互和业务逻辑

### 标准回答

前端页面一般可以拆成三层理解。HTML 负责页面结构，比如按钮、输入框、列表、图片这些元素怎么组织；CSS 负责样式，比如颜色、布局、边距、字体、动画；JavaScript 负责交互和逻辑，比如点击按钮后发送请求、拿到数据后更新页面、处理表单输入、做条件渲染等。  
所以如果让我用一句话概括，就是：**HTML 搭骨架，CSS 做外观，JavaScript 让页面动起来。**

---

## 2. 什么是响应式页面

### 可以直接记这 2 句

1. 数据变了，页面自动更新  
2. 不需要手动操作 DOM

### 标准回答

响应式页面的核心思想是：页面显示依赖数据，只要数据变了，页面就自动重新渲染。开发者不需要每次都自己手动去找 DOM、改文本、改样式，而是把状态交给框架管理。  
在 Vue 里，这种能力主要来自响应式系统，比如 `ref`、`reactive`、`computed` 这些。

---

## 3. Vue 的响应式原理你怎么理解

### 可以直接记这 4 点

1. Vue 会把状态变成可追踪的数据  
2. 组件渲染时会记录“依赖了哪些状态”  
3. 状态变化后，Vue 会通知相关组件更新  
4. 所以页面会自动刷新

### 标准回答

Vue 的响应式原理，本质上可以理解成“状态可追踪 + 依赖自动更新”。也就是当你把数据放进 `ref` 或 `reactive` 之后，Vue 会帮你追踪这个数据的变化；组件在渲染时如果依赖了这些数据，Vue 也会记住这种依赖关系。  
后面一旦数据发生变化，Vue 就会通知对应组件重新渲染，所以页面会自动更新，而不需要手动操作 DOM。

---

## 4. props / emits 是什么

### 可以直接记这 2 句

1. `props` 是父传子  
2. `emits` 是子通知父

### 标准回答

在 Vue 组件通信里，`props` 用来做父组件给子组件传值，`emits` 用来做子组件向父组件发送事件。  
比如父组件把输入值、历史消息、loading 状态传给子组件，这属于 `props`；子组件里点击按钮后，通过 `emit('send')` 通知父组件执行发送逻辑，这属于 `emits`。

### 结合项目怎么说

你现在这个项目里，像侧边栏、问答面板、模拟面试面板这些组件，很多数据都是父组件通过 `props` 传进去的，而像“发送消息”“加载用户”“上传文件”这些动作，则是子组件通过 `emit` 抛给父组件或 composable 去处理。

---

## 5. ref / computed / watch 分别是什么

### 可以直接记这 3 句

1. `ref` 放可变状态  
2. `computed` 放派生结果  
3. `watch` 监听状态变化后执行副作用

### 标准回答

`ref` 用来定义最基础的响应式状态，比如输入框内容、当前模式、当前用户信息。`computed` 用来基于已有状态推导出新的结果，比如“当前显示的是问答历史还是面试历史”。`watch` 用来监听某些状态变化，并在变化后执行额外逻辑，比如发请求、同步本地状态、或者根据变化触发其他处理。  
你可以把它们理解成：**ref 是原始数据，computed 是计算结果，watch 是监听后的动作。**

### 项目里的对应理解

你项目里的 `candidate`、`mode`、`qaInput` 这些属于 `ref`；`currentHistory`、`showQaWelcome` 这些属于 `computed`；如果后面有监听某些状态变化再做附加逻辑，那就是 `watch` 的使用场景。

---

## 6. 生命周期是什么

### 可以直接记这 3 个最常见的

1. 组件创建后初始化  
2. 组件挂载后执行页面相关逻辑  
3. 组件销毁前做清理

### 标准回答

生命周期可以理解成组件从创建到销毁的整个过程。在不同阶段，我们会做不同事情。比如组件挂载后，可以发初始化请求、获取数据；组件销毁前，可以清理定时器、取消请求、移除监听器。  
在面试里不需要把所有生命周期全背下来，但至少要知道：**什么时候初始化，什么时候清理资源。**

### 项目里怎么对应

你这个项目里，页面首次进入时会在挂载后做 `bootstrap` 初始化；如果页面销毁时还有未完成的流式请求，也会做中断清理。

---

## 7. 条件渲染、列表渲染、表单绑定、事件处理

### 可以直接记这 4 个关键词

1. 条件渲染：`v-if`  
2. 列表渲染：`v-for`  
3. 表单绑定：`v-model` 或输入事件  
4. 事件处理：`@click`、`@input`、`@submit`

### 标准回答

条件渲染是根据状态决定显示什么内容，比如当前是问答模式还是模拟面试模式；列表渲染是把数组数据循环渲染成消息列表、文件列表等；表单绑定是把输入框内容和状态关联起来；事件处理则是点击、输入、提交后触发方法。  
这四个几乎是所有前端页面最常用的基础能力。

### 项目里怎么说

你项目里：
- 模式切换用的是条件渲染
- 聊天历史展示本质上是列表渲染
- 输入框和上传控件属于表单绑定
- 发送按钮、开始面试按钮、导入知识库按钮都是事件处理

---

# 三、HTTP、接口和前后端交互

## 8. 前后端怎么交互

### 可以直接记这 4 步

1. 前端发 HTTP 请求  
2. 后端接收并处理  
3. 后端返回响应  
4. 前端解析结果并更新页面

### 标准回答

前后端交互本质上是通过 HTTP 接口完成的。前端在用户操作时发起 GET 或 POST 请求，后端接收请求后执行业务逻辑，比如查数据、处理文件、调用模型，然后把结果通过 JSON 或流式响应返回给前端。前端拿到这些数据后，再更新自己的状态并重新渲染页面。

---

## 9. GET 和 POST 有什么区别

### 可以直接记这 4 点

1. GET 一般用于获取数据  
2. POST 一般用于提交数据  
3. GET 参数常放在 URL 上  
4. POST 参数通常放在请求体里

### 标准回答

GET 一般用于获取数据，比如获取用户信息、获取页面初始化数据；POST 一般用于提交数据，比如发送消息、上传文件、开始面试。  
从形式上看，GET 的参数常放在 URL 查询参数里，POST 的数据通常放在请求体里。实际开发里最重要的是：**GET 更偏读取，POST 更偏提交和修改。**

---

## 10. JSON 是什么

### 可以直接记这 2 句

1. JSON 是一种数据交换格式  
2. 前后端经常用它传对象和数组

### 标准回答

JSON 可以理解成前后端之间最常见的一种数据格式。它适合表示对象、数组、字符串、数字、布尔值这些结构化数据。  
比如后端返回用户状态、历史消息、天气信息时，通常就是 JSON；前端发送普通表单数据或业务参数时，也经常会把对象转成 JSON 发给后端。

---

## 11. FormData 是什么，什么时候用

### 可以直接记这 2 句

1. FormData 适合提交文件和表单  
2. 上传文件时通常不用 JSON，而用 FormData

### 标准回答

FormData 是浏览器提供的一种表单数据构造方式，特别适合上传文件。因为文件本身不是普通字符串对象，直接走 JSON 不方便，所以像简历上传、知识库文档上传这种场景，前端一般会用 FormData，把文件对象附加进去，再发给后端处理。

---

## 12. 文件上传这条链路怎么讲

### 可以直接记这 5 步

1. 用户选择文件  
2. 前端拿到文件对象  
3. 用 FormData 组装请求  
4. 后端接收并解析文件  
5. 返回结果，前端更新页面

### 标准回答

文件上传的完整链路是：用户先在前端选择文件，前端拿到这个文件对象后，用 FormData 把它组装成请求体，然后调用后端上传接口。后端收到文件后，会先保存或解析它，再返回处理结果。前端拿到结果后，把新的状态写回页面。  
你项目里的简历上传和知识库导入，本质上就是这条链路。

---

## 13. 流式响应和普通响应的区别

### 可以直接记这 2 句

1. 普通响应是一次性返回完整结果  
2. 流式响应是后端持续返回数据块，前端边读边渲染

### 标准回答

普通响应更像“前端发一次请求，后端算完后一次性返回一个完整 JSON”。流式响应则不一样，它不是等所有结果都准备好再返回，而是后端把响应保持打开，持续往外推送文本块，前端边接收边展示。  
这非常适合聊天、AI 回复这类场景，因为用户能更快看到中间结果。

---

# 四、浏览器调试和联调能力

## 14. 浏览器里怎么 debug

### 你至少要会这 5 个

1. 看 Console  
2. 看 Network  
3. 看请求参数和响应内容  
4. 看报错堆栈  
5. 打断点调试

### 标准回答

前端调试我主要会用浏览器开发者工具。第一步一般先看 Console，有没有语法错误、运行时错误、接口报错；第二步看 Network，请求有没有发出去、状态码是多少、请求参数和返回结果对不对；第三步如果逻辑复杂，就会在 Sources 里打断点，看变量在执行到某一步时到底是什么值。  
也就是说，调试时我通常按“先看报错，再看请求，再看状态和执行过程”这条线去排查。

---

## 15. Console 和 Network 分别看什么

### 可以直接记这 2 句

1. Console 主要看报错和日志  
2. Network 主要看请求有没有成功、参数对不对、响应是什么

### 标准回答

Console 更适合看代码执行中的报错、警告和开发时主动打印的日志；Network 更适合看具体接口请求，比如 URL 对不对、请求方法对不对、请求体里传了什么、后端返回了什么、状态码是不是 200。  
如果页面没反应，通常我会先看 Network；如果页面直接报错白屏，通常先看 Console。

---

## 16. 页面没数据时，你怎么排查

### 可以直接记这 5 步

1. 先看 Console 有没有报错  
2. 再看 Network 请求有没有发出去  
3. 看请求参数和接口返回是否正确  
4. 看前端有没有把结果写进状态  
5. 看组件是不是正确使用了这些状态

### 标准回答

如果页面没数据，我一般按这条链路排查：先看 Console 有没有明显报错；再看 Network 里接口请求有没有正常发出去；如果请求发出去了，就看请求参数和返回结果是不是对的；如果后端返回没问题，就继续看前端是否正确把数据写进响应式状态；最后再看组件是不是正确消费了这些状态。  
这样基本就能把问题缩小到“接口问题、状态问题还是渲染问题”。

---

## 17. 前后端联调怎么做

### 可以直接记这 5 步

1. 先确认接口文档或返回格式  
2. 前端按约定发请求  
3. 用浏览器看实际请求和响应  
4. 发现不一致就和后端一起对字段  
5. 最后把状态更新链路跑通

### 标准回答

前后端联调时，我通常先确认接口地址、请求方法、参数格式和返回结构。然后前端按这个约定去发请求，再用 Network 看实际请求和响应。  
如果发现字段名、数据结构或者类型不一致，就和后端一起对齐接口约定。等接口返回正确后，再继续检查前端状态更新和页面渲染链路，确保整个功能从请求到展示都打通。

---

# 五、工程化和项目运行

## 18. `npm install`、`npm run dev`、`npm run build` 分别是什么

### 可以直接记这 3 句

1. `npm install` 安装依赖  
2. `npm run dev` 本地开发启动  
3. `npm run build` 生成生产环境静态资源

### 标准回答

`npm install` 是根据 `package.json` 安装项目依赖；`npm run dev` 是启动本地开发环境，方便边改代码边看效果；`npm run build` 是把前端项目构建成生产环境可部署的静态资源。  
所以如果面试官问“页面怎么跑起来”，你至少要能说出先装依赖，再启动开发服务，最终打包构建。

---

## 19. 页面怎么跑起来，这个问题怎么答

### 可以直接记这 4 步

1. 进入前端目录  
2. 执行 `npm install`  
3. 执行 `npm run dev`  
4. 浏览器访问本地开发地址

### 标准回答

如果是前端本地开发，我会先进入前端目录，执行 `npm install` 安装依赖，再执行 `npm run dev` 启动开发服务。启动后，Vite 会给出一个本地地址，比如 `http://localhost:5173`，浏览器访问这个地址就能看到页面。  
如果是部署环境，则会先执行 `npm run build` 生成静态资源，再交给后端或静态服务器去托管。

---

## 20. 环境变量和接口地址配置是什么

### 可以直接记这 3 点

1. 不同环境的接口地址可能不同  
2. 不应该把地址写死在代码里  
3. 应该通过环境变量配置

### 标准回答

开发环境、测试环境、生产环境的后端地址通常不一样，所以接口地址不应该直接硬编码在页面逻辑里，而应该通过环境变量配置。  
这样做的好处是切换环境更方便，也更符合工程化开发规范。比如本地开发时请求本地后端，部署后则请求线上接口。

---

## 21. 跨域是什么

### 可以直接记这 2 句

1. 前端和后端不在同一个源时，浏览器会做跨域限制  
2. 一般通过后端配置 CORS 解决

### 标准回答

跨域可以简单理解成：前端页面和后端接口不在同一个协议、域名或端口下时，浏览器会限制这种请求。开发时最常见的解决方式是后端配置 CORS，允许前端开发地址访问接口。  
所以如果本地前端跑在 `5173` 端口、后端跑在 `8000` 端口，就很容易碰到跨域问题。

---

## 22. 接口返回格式校验是什么意思

### 可以直接记这 2 句

1. 看后端返回的数据结构是不是符合预期  
2. 避免前端按错字段导致页面异常

### 标准回答

接口返回格式校验，本质上就是确认后端返回的数据结构和前端代码里使用的数据结构一致。比如前端以为后端会返回 `candidate.history`，但实际返回的是 `candidate.qa_history`，那页面就会出问题。  
所以联调时一定要核对字段名、层级结构、类型和值是不是符合预期。

---

## 23. 页面状态更新链路怎么讲

### 可以直接记这 5 步

1. 用户操作  
2. 前端发请求  
3. 后端返回结果  
4. 前端写入响应式状态  
5. 组件自动重渲染

### 标准回答

页面状态更新链路，本质上就是：用户操作触发事件函数，前端发请求给后端，后端返回结果，前端把结果写进 `ref` 或 `computed` 依赖的状态里，然后 Vue 自动触发相关组件重新渲染。  
你只要把这条链路讲顺，面试官通常就会觉得你理解了“交互式前端页面是怎么工作的”。

---

# 六、Git 基础

## 24. Git 是什么

### 可以直接记这 2 句

1. Git 是版本控制工具  
2. 用来管理代码历史和多人协作

### 标准回答

Git 是一个版本控制工具，主要用来管理代码的修改历史，也方便多人协作开发。你上传代码到 GitHub，本质上也是在用 Git，只不过 Git 是本地和远程仓库之间同步代码的工具，GitHub 是托管平台。

---

## 25. 至少要会哪些 Git 命令

### 这几个最重要

- `git clone`：把远程仓库拉到本地
- `git pull`：拉取最新代码
- `git add`：把改动加入暂存区
- `git commit -m "..."`：提交本次改动
- `git push`：把本地提交推到远程
- `git branch`：查看或管理分支

### 面试怎么说

如果面试官问你会不会 Git，你不用讲太复杂，就说你熟悉基本的版本管理流程，比如 clone 项目、pull 最新代码、在自己的分支上开发、add 和 commit 保存修改、最后 push 到远程仓库。

---

# 七、计算机通用能力

## 26. 数据结构和算法需要掌握到什么程度

### 你至少要会这些

1. 数组、链表、栈、队列、哈希表、树这些基本概念  
2. 常见操作的思路  
3. 会做一些简单遍历、查找、去重、统计类题目  
4. 知道时间复杂度的大致概念

### 标准回答

实习前端面试一般不会要求很强的算法能力，但你至少要知道常见数据结构是什么、适合解决什么问题，也要能做一些基础题，比如数组处理、哈希去重、字符串处理、树遍历这类。  
核心不是一定刷很多难题，而是你要能体现出基本的逻辑能力和编码思维。

---

## 27. 代码怎么拆模块

### 可以直接记这 3 点

1. 展示层和逻辑层分开  
2. 公共能力抽出来复用  
3. 一个模块只负责一类事情

### 标准回答

代码拆模块时，我一般会按职责拆分。页面或组件负责展示和交互，composable 或 service 负责业务逻辑，API 层负责接口请求。  
这样做的好处是职责更清晰，后续修改某一部分时不容易牵一发动全身，也更方便复用和维护。

---

## 28. 一个功能从需求到实现怎么走

### 可以直接记这 5 步

1. 先理解需求  
2. 拆成前端、后端、数据几个部分  
3. 先设计接口和状态流转  
4. 再分别实现和联调  
5. 最后测试和修 bug

### 标准回答

如果要从 0 到 1 做一个功能，我一般会先理解业务需求和用户操作路径，再把它拆成前端页面交互、后端接口逻辑、数据结构这几个部分。然后先明确接口输入输出和状态流转，再进入具体实现。  
功能开发完成后，还需要做联调、测试和 bug 修复，保证整条链路真的能跑通。

---

## 29. 遇到 bug 怎么定位

### 可以直接记这 5 步

1. 先复现问题  
2. 确认是前端还是后端  
3. 看日志和报错  
4. 缩小范围  
5. 修复后再回归验证

### 标准回答

我排查 bug 时一般先确保问题能稳定复现，然后先判断问题更像出在前端、后端还是接口联调层。接着去看 Console、Network、后端日志，逐步缩小范围。  
修复后我还会再走一遍完整流程，确认没有引入新的问题。也就是说，我会把排查 bug 当成一个“定位原因 -> 修复 -> 回归验证”的过程。

---

# 八、AI 工具怎么说，才不会显得你只会依赖

## 30. 你怎么借助 AI 工具提高开发效率

### 可以直接记这 4 点

1. 用 AI 辅助搭框架和写初版代码  
2. 用 AI 辅助查思路、补文档、排查问题  
3. 关键逻辑和接口一定自己验证  
4. 不会直接无脑复制结果

### 标准回答

我会把 AI 工具当成开发加速器，而不是替代自己的思考。比如在搭页面骨架、补通用代码、查资料、排查报错时，我会借助 ChatGPT、Copilot、Claude 这类工具提高效率。  
但对于关键逻辑、接口字段、状态流转和最终效果，我一定会自己验证，比如看 Network、看控制台、跑完整链路、检查输入输出是否符合预期，而不是直接无脑复制 AI 结果。

---

## 31. 如果面试官怀疑你“是不是不会代码，只会用 AI”

### 你可以这样答

我会承认自己会积极使用 AI 工具提高效率，但我不会把它当成黑盒。因为真正把功能做出来，关键不是“让 AI 生成一段代码”，而是你能不能把需求拆清楚、知道这段代码应该放在哪、怎么和现有项目结构对接、怎么联调、怎么排查错误、怎么验证结果是否正确。  
我现在最有优势的一点是，学习速度比较快，能借助 AI 工具快速进入问题，再通过联调和验证把功能真正落地。

---

# 九、你现在最需要的表达能力

## 32. 你不是全能型前端，面试时怎么表达更合适

### 可以直接记这 4 点

1. 我基础还在持续补强  
2. 但我已经能完成真实项目交付  
3. 我能和后端联调、能排查问题  
4. 学习速度快，能快速跟上团队节奏

### 标准回答

我目前确实不是那种所有前端知识点都非常熟的类型，但我已经参与并完成过真实项目里的页面开发、接口联调和功能打通。对我来说，优势在于学习速度快、愿意补基础，也能借助 AI 工具提高开发效率，同时自己完成验证和问题排查。  
所以我更希望面试官把我看成一个“基础在补强、但已经具备实际开发和交付能力”的候选人。

---

# 十、一天复习安排建议

## 上午：前端基础和 Vue

重点看：

1. HTML / CSS / JavaScript 分工  
2. Vue 响应式原理  
3. props / emits / ref / computed / watch  
4. 生命周期  
5. 条件渲染、列表渲染、表单绑定、事件处理

目标：

- 能口头讲清楚这些概念
- 能举出你项目里的对应例子

---

## 下午：HTTP、联调、调试、工程化

重点看：

1. HTTP 请求流程  
2. GET / POST / JSON / FormData  
3. 文件上传和前后端交互  
4. Console / Network / 断点调试  
5. npm install / npm run dev / npm run build  
6. 跨域、环境变量、接口地址配置

目标：

- 能讲清楚“页面怎么跑起来”
- 能讲清楚“请求怎么发、数据怎么回来、页面怎么更新”
- 能讲清楚“报错怎么排查”

---

## 晚上：表达、Git、AI 工具、自我包装

重点看：

1. Git 基本命令  
2. 一个功能从需求到实现怎么走  
3. 遇到 bug 怎么定位  
4. 怎么说自己使用 AI 工具  
5. 怎么说自己虽然不是全能型前端，但学习快、能交付

目标：

- 把表达练顺
- 把你讲项目的语气练自然

---

# 十一、最后速记版

- HTML 管结构，CSS 管样式，JavaScript 管交互  
- Vue 核心是响应式：状态变了，页面自动更新  
- `props` 父传子，`emits` 子传父  
- `ref` 放状态，`computed` 放派生值，`watch` 做监听副作用  
- 前后端通过 HTTP 交互，普通数据走 JSON，文件上传走 FormData  
- 页面没数据时：先看 Console，再看 Network，再看状态有没有更新  
- 工程化最基础：`npm install`、`npm run dev`、`npm run build`  
- Git 最基础：`clone / pull / add / commit / push / branch`  
- 你要体现的不是“全都会”，而是“能学、能联调、能交付、能排查”

---

# 十二、你今天最少要练会的 6 句

1. “这个页面的数据更新，本质上是前端发请求，后端返回结果，前端更新响应式状态，Vue 自动重新渲染。”  
2. “文件上传我这边是通过 FormData 把文件传给后端接口，后端处理完后再把结果返回给前端。”  
3. “排查问题时我一般先看 Console，再看 Network，再确认状态有没有正确写回页面。”  
4. “我会把页面展示、业务逻辑和接口请求分层，这样结构更清晰，也方便联调和维护。”  
5. “我会使用 AI 工具提高开发效率，但关键逻辑和接口结果我会自己验证。”  
6. “我虽然不是全能型前端，但学习速度快，已经能完成真实项目里的页面开发、联调和问题排查。”  

如果你今天时间真的很紧，先把这 6 句练顺，再去背项目问答，成功率会明显高很多。
