# 前端笔试高频题整理

## 一、选择题

下面这些题型是前端笔试里非常高频的。

### 1. 关于 `let`、`const`、`var` 的说法，正确的是？

A. `var` 具有块级作用域  
B. `let` 和 `const` 具有块级作用域  
C. `const` 声明的对象不能修改属性  
D. `let` 会变量提升且可在声明前直接使用

答案：`B`

解析：

- `var` 是函数作用域，不是块级作用域。
- `let`、`const` 是块级作用域。
- `const` 保证的是绑定不变，不是对象内部属性绝对不可改。
- `let` 虽然会进入暂时性死区，但不能在声明前访问。

### 2. 以下代码输出什么？

```js
for (var i = 0; i < 3; i++) {
  setTimeout(() => console.log(i), 0);
}
```

A. `0 1 2`  
B. `3 3 3`  
C. `undefined undefined undefined`  
D. 报错

答案：`B`

解析：

- `var` 没有块级作用域，三个回调共享同一个 `i`。
- 定时器执行时循环已结束，`i = 3`。

### 3. 以下哪一个属于微任务？

A. `setTimeout`  
B. `setInterval`  
C. `Promise.then`  
D. DOM 事件回调

答案：`C`

解析：

- `Promise.then`、`catch`、`finally` 属于微任务。
- `setTimeout`、`setInterval` 通常归为宏任务。

### 4. `===` 和 `==` 的区别是？

A. 没区别  
B. `==` 不会做类型转换  
C. `===` 会同时比较值和类型  
D. `===` 只比较引用地址

答案：`C`

### 5. 以下哪个方法不会修改原数组？

A. `push()`  
B. `splice()`  
C. `sort()`  
D. `map()`

答案：`D`

### 6. 关于闭包，正确的是？

A. 闭包会立即销毁外层变量  
B. 闭包可以访问其词法作用域中的变量  
C. 闭包只能在类中使用  
D. 闭包和作用域无关

答案：`B`

### 7. `display: none` 和 `visibility: hidden` 的区别是？

A. 都会保留布局空间  
B. 都不会保留布局空间  
C. `display: none` 不保留空间，`visibility: hidden` 保留空间  
D. `visibility: hidden` 会让元素从 DOM 中移除

答案：`C`

### 8. CSS 选择器优先级中，哪个更高？

A. 元素选择器  
B. 类选择器  
C. ID 选择器  
D. 通配符选择器

答案：`C`

### 9. `position: absolute` 的定位基准通常是？

A. 浏览器窗口  
B. 文档流  
C. 最近的非 `static` 定位祖先元素  
D. body

答案：`C`

### 10. HTTP 中通常用于“获取资源”的方法是？

A. `POST`  
B. `PUT`  
C. `GET`  
D. `DELETE`

答案：`C`

### 11. 下列哪个状态码表示“未找到资源”？

A. `200`  
B. `301`  
C. `403`  
D. `404`

答案：`D`

### 12. CORS 主要解决什么问题？

A. 浏览器缓存问题  
B. 跨域资源访问问题  
C. 服务器宕机问题  
D. CSS 冲突问题

答案：`B`

### 13. `localStorage` 和 `sessionStorage` 的主要区别是？

A. `localStorage` 只能存数字  
B. `sessionStorage` 可跨标签页永久保存  
C. `localStorage` 持久化更久，`sessionStorage` 通常随会话结束清除  
D. 两者都只能由后端访问

答案：`C`

### 14. Vue 中 `computed` 和 `methods` 的区别，正确的是？

A. `computed` 不能访问响应式数据  
B. `computed` 基于依赖缓存，`methods` 每次渲染都会重新执行  
C. `methods` 会自动缓存  
D. 两者完全一样

答案：`B`

### 15. Vue 中监听数据变化更适合用哪个？

A. `watch`  
B. `slot`  
C. `key`  
D. `provide`

答案：`A`

### 16. Vue 中 `v-model` 本质上常用于什么？

A. 单向绑定  
B. 双向绑定  
C. 样式绑定  
D. 路由绑定

答案：`B`

### 17. `v-for` 中为什么建议加 `key`？

A. 为了让页面更好看  
B. 为了避免请求跨域  
C. 为了帮助框架更高效、准确地更新列表  
D. 没有实际作用

答案：`C`

### 18. 下列哪项更适合做前端性能优化？

A. 把所有资源都做成超大单文件  
B. 减少不必要的重渲染和重复请求  
C. 所有图片都使用原图不压缩  
D. 删除缓存头

答案：`B`

### 19. 以下哪个说法是正确的？

A. `null == undefined` 为 `false`  
B. `null == undefined` 为 `true`  
C. `null === undefined` 为 `true`  
D. `NaN === NaN` 为 `true`

答案：`B`

### 20. `typeof null` 的结果是什么？

A. `null`  
B. `undefined`  
C. `object`  
D. `number`

答案：`C`

这是经典高频坑题。

---

## 二、编程题

编程题一般不会太偏，主要考 JavaScript 基础、数组对象处理、异步控制和手写能力。

### 1. 手写数组去重

题目：

```js
const arr = [1, 2, 2, 3, 4, 4, 5];
```

要求：返回去重后的数组。

参考答案：

```js
function unique(arr) {
  return [...new Set(arr)];
}

console.log(unique([1, 2, 2, 3, 4, 4, 5]));
```

### 2. 手写数组扁平化

题目：

```js
const arr = [1, [2, [3, 4], 5], 6];
```

要求：输出 `[1, 2, 3, 4, 5, 6]`

参考答案：

```js
function flatten(arr) {
  return arr.reduce((result, item) => {
    if (Array.isArray(item)) {
      return result.concat(flatten(item));
    }
    return result.concat(item);
  }, []);
}

console.log(flatten([1, [2, [3, 4], 5], 6]));
```

### 3. 手写防抖

题目：实现 `debounce`

参考答案：

```js
function debounce(fn, delay) {
  let timer = null;
  return function (...args) {
    clearTimeout(timer);
    timer = setTimeout(() => {
      fn.apply(this, args);
    }, delay);
  };
}
```

常见场景：

- 搜索框输入
- 窗口 resize
- 表单频繁校验

### 4. 手写节流

题目：实现 `throttle`

参考答案：

```js
function throttle(fn, delay) {
  let lastTime = 0;
  return function (...args) {
    const now = Date.now();
    if (now - lastTime >= delay) {
      fn.apply(this, args);
      lastTime = now;
    }
  };
}
```

常见场景：

- 滚动监听
- 鼠标移动
- 高频点击控制

### 5. 手写深拷贝

题目：实现基础版 `deepClone`

参考答案：

```js
function deepClone(obj) {
  if (obj === null || typeof obj !== "object") {
    return obj;
  }

  const result = Array.isArray(obj) ? [] : {};

  for (const key in obj) {
    if (Object.prototype.hasOwnProperty.call(obj, key)) {
      result[key] = deepClone(obj[key]);
    }
  }

  return result;
}
```

注意点：

- 面试时要主动说明：完整版还要考虑 `Date`、`RegExp`、循环引用、`Map`、`Set` 等情况。

### 6. 实现 `sleep`

题目：实现一个 `sleep(ms)`，可配合 `await` 使用。

参考答案：

```js
function sleep(ms) {
  return new Promise((resolve) => {
    setTimeout(resolve, ms);
  });
}

async function test() {
  console.log("start");
  await sleep(1000);
  console.log("end");
}
```

### 7. 实现函数柯里化

题目：

```js
add(1)(2)(3) // 6
```

参考答案：

```js
function add(a) {
  return function (b) {
    return function (c) {
      return a + b + c;
    };
  };
}

console.log(add(1)(2)(3));
```

### 8. 统计字符串中每个字符出现次数

题目：

```js
const str = "aabbbc";
```

参考答案：

```js
function countChars(str) {
  const map = {};
  for (const ch of str) {
    map[ch] = (map[ch] || 0) + 1;
  }
  return map;
}

console.log(countChars("aabbbc"));
```

### 9. 找出数组中出现次数最多的元素

参考答案：

```js
function findMostFrequent(arr) {
  const map = {};
  let maxCount = 0;
  let result = null;

  for (const item of arr) {
    map[item] = (map[item] || 0) + 1;
    if (map[item] > maxCount) {
      maxCount = map[item];
      result = item;
    }
  }

  return result;
}

console.log(findMostFrequent([1, 2, 2, 3, 3, 3, 4]));
```

### 10. 实现 Promise 串行执行

题目：有一组异步任务，要求按顺序执行。

参考答案：

```js
async function runTasks(tasks) {
  const results = [];
  for (const task of tasks) {
    const result = await task();
    results.push(result);
  }
  return results;
}
```

### 11. 手写发布订阅模式

参考答案：

```js
class EventEmitter {
  constructor() {
    this.events = {};
  }

  on(event, fn) {
    if (!this.events[event]) {
      this.events[event] = [];
    }
    this.events[event].push(fn);
  }

  emit(event, ...args) {
    if (!this.events[event]) {
      return;
    }
    this.events[event].forEach((fn) => fn(...args));
  }

  off(event, fn) {
    if (!this.events[event]) {
      return;
    }
    this.events[event] = this.events[event].filter((item) => item !== fn);
  }
}
```

### 12. 给定一个数组，按 `id` 去重对象

题目：

```js
const arr = [
  { id: 1, name: "a" },
  { id: 2, name: "b" },
  { id: 1, name: "c" }
];
```

参考答案：

```js
function uniqueById(arr) {
  const map = new Map();
  for (const item of arr) {
    if (!map.has(item.id)) {
      map.set(item.id, item);
    }
  }
  return [...map.values()];
}
```

---

## 三、问答题

这类题通常要求你解释概念、对比方案、说明原理。比起死记硬背，更重要的是回答有结构。

### 1. 什么是闭包？有什么应用场景？

参考答案：

闭包就是函数和它声明时所在词法作用域的组合。即使外层函数已经执行完，内部函数仍然可以访问外层作用域中的变量。

常见应用：

- 封装私有变量
- 函数工厂
- 事件回调
- 防抖、节流
- 缓存数据

### 2. 什么是事件循环？

参考答案：

JavaScript 是单线程的，任务执行时会分为同步任务、宏任务和微任务。同步代码先进入主线程执行；遇到异步任务时，回调会进入任务队列。当前执行栈清空后，会先处理微任务，再处理下一个宏任务。

常见微任务：

- `Promise.then`
- `catch`
- `finally`

常见宏任务：

- `setTimeout`
- `setInterval`
- I/O

### 3. `localStorage`、`sessionStorage`、cookie` 的区别是什么？

参考答案：

- `localStorage`
  持久化存储，除非主动删除，否则数据通常一直存在。

- `sessionStorage`
  会话级存储，页面会话结束后通常清除。

- `cookie`
  可由浏览器在请求时自动带给服务器，常用于登录态、会话标识等。

补充：

- `localStorage` 和 `sessionStorage` 容量通常比 cookie 更大。
- cookie 会随着请求发送，过多会影响请求开销。

### 4. `GET` 和 `POST` 的区别是什么？

参考答案：

- `GET` 主要用于获取资源。
- `POST` 主要用于提交数据。
- `GET` 参数通常拼在 URL 上。
- `POST` 数据通常放在请求体中。
- `GET` 更适合幂等查询。
- `POST` 更适合创建、提交类操作。

### 5. 什么是跨域？怎么解决？

参考答案：

跨域本质上是浏览器同源策略限制。协议、域名、端口任意一个不同，都可能构成跨域。

常见解决方案：

- 后端设置 CORS 响应头
- 开发环境使用代理转发
- Nginx 反向代理

现在最常见、最标准的是后端配置 CORS。

### 6. 说一下浏览器从输入 URL 到页面渲染的大致过程。

参考答案：

1. 输入 URL
2. DNS 解析得到 IP
3. 建立 TCP 连接
4. 发送 HTTP 请求
5. 服务器返回 HTML
6. 浏览器解析 HTML 构建 DOM
7. 解析 CSS 构建 CSSOM
8. 合并生成渲染树
9. 布局和绘制
10. 执行 JavaScript，可能触发重新渲染

### 7. 什么是重排和重绘？

参考答案：

- 重排
  元素几何属性变化，导致布局重新计算。

- 重绘
  元素外观变化，但不影响布局。

重排通常比重绘开销更大。

优化思路：

- 减少频繁操作 DOM
- 批量修改样式
- 避免频繁读取会触发布局的属性

### 8. Vue 中 `computed` 和 `watch` 有什么区别？

参考答案：

- `computed`
  更适合根据已有响应式数据推导出一个新值，具备缓存能力。

- `watch`
  更适合监听某个数据变化后执行副作用逻辑，比如发请求、打印日志、做异步处理。

一句话记忆：

- 算值用 `computed`
- 监听副作用用 `watch`

### 9. Vue 生命周期常用哪些？

参考答案：

常见的有：

- `onMounted`
  组件挂载后执行，常用于发初始化请求、获取 DOM。

- `onUpdated`
  组件更新后执行。

- `onUnmounted`
  组件卸载后执行，常用于清理定时器、事件监听。

### 10. `v-if` 和 `v-show` 的区别是什么？

参考答案：

- `v-if`
  通过真正的创建和销毁节点来控制显示隐藏。

- `v-show`
  通过 CSS 的 `display` 控制显示隐藏，节点始终存在。

使用建议：

- 频繁切换用 `v-show`
- 条件很少变化用 `v-if`

### 11. 前端性能优化有哪些常见手段？

参考答案：

- 减少不必要的请求
- 图片压缩和懒加载
- 资源缓存
- 防抖和节流
- 减少重排重绘
- 组件按需加载
- 拆包和压缩构建产物
- 避免无意义的重复渲染

### 12. 什么是防抖和节流？区别是什么？

参考答案：

- 防抖
  事件触发后一段时间内如果再次触发，就重新计时。适合“最后一次触发才执行”的场景。

- 节流
  在一段时间内只允许执行一次。适合高频事件定期触发的场景。

### 13. 什么是原型和原型链？

参考答案：

在 JavaScript 中，对象可以通过原型链继承属性和方法。每个对象内部都可以关联到它的原型对象，查找属性时如果本身没有，就沿着原型链向上查找。

### 14. 什么是深拷贝和浅拷贝？

参考答案：

- 浅拷贝
  只复制第一层，嵌套对象仍共享引用。

- 深拷贝
  会递归复制嵌套对象，拷贝后相互独立。

### 15. 为什么 `v-for` 不建议用索引当 `key`？

参考答案：

因为列表增删或顺序变化时，索引会变化，可能导致节点复用错误，出现渲染异常或状态错乱。更推荐使用稳定、唯一的业务 id。

### 16. 前后端联调时常见问题有哪些？

参考答案：

- 接口路径写错
- 请求方法不一致
- 参数字段名不一致
- 跨域问题
- 返回结构和前端预期不一致
- 文件上传格式不对
- 状态码处理不完整

### 17. 什么是语义化 HTML？

参考答案：

语义化 HTML 指的是使用更符合内容含义的标签，比如：

- `header`
- `main`
- `section`
- `article`
- `footer`

好处：

- 结构更清晰
- 更利于维护
- 更利于 SEO
- 更利于无障碍访问

### 18. 为什么 CSS 会出现样式冲突？

参考答案：

主要和以下因素有关：

- 选择器优先级
- 样式声明顺序
- 继承
- `!important`

所以排查样式问题时，重点看：

- 谁的优先级更高
- 谁后加载
- 是否有继承或覆盖

---

## 四、前端笔试最该优先刷的知识点

如果时间不够，建议优先刷下面这些：

### 第一优先级

- `let / const / var`
- 闭包
- 作用域
- 事件循环
- Promise
- 防抖 / 节流
- 数组方法
- 深浅拷贝

### 第二优先级

- CSS 盒模型
- 选择器优先级
- `display` / `position`
- 重排重绘
- `GET / POST`
- 跨域
- `localStorage / sessionStorage / cookie`

### 第三优先级

- Vue 的 `computed`
- `watch`
- 生命周期
- `v-if / v-show`
- `v-for + key`
- 前后端联调常见问题

---

## 五、刷题建议

### 1. 选择题

重点刷“概念区别”和“经典坑题”，比如：

- `var` / `let`
- `==` / `===`
- 宏任务 / 微任务
- `display: none` / `visibility: hidden`

### 2. 编程题

建议至少能手写：

- 去重
- 扁平化
- 防抖
- 节流
- 深拷贝
- Promise 串行

### 3. 问答题

回答时尽量按这个结构：

1. 先给定义
2. 再讲区别
3. 最后讲应用场景

这样会显得更清晰。

---

## 六、面试前最后速记

如果你只剩最后半小时，可以重点背这几句：

- 闭包：函数和其词法作用域的组合，内部函数可以访问外部变量。
- 事件循环：同步任务先执行，执行栈清空后先跑微任务，再跑宏任务。
- 防抖：多次触发只执行最后一次。
- 节流：一段时间内只执行一次。
- `computed`：适合算值，有缓存。
- `watch`：适合监听变化后做副作用。
- `v-if`：真正销毁/创建节点。
- `v-show`：通过 `display` 控制显示隐藏。
- 跨域：浏览器同源策略限制，通常通过 CORS 或代理解决。

---

这份文档适合你在笔试前先刷一遍。如果你愿意，我下一步可以继续帮你再补一份：

- `前端笔试答案速背版.md`

也就是把上面这些题再压缩成更适合最后 10 分钟突击背诵的版本。
